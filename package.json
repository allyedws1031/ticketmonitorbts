{
  "name": "bts-ticket-monitor-brasil",
  "version": "4.0.0",
  "private": true,
  "description": "Monitor BTS otimizado para alto tráfego, com persistência, histórico e painel operacional",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium",
    "check": "node --check server.js && node --check monitor.js && node --check public/app.js && node --check public/admin.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  }
}