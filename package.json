{
  "name": "bts-ticket-monitor-brasil",
  "version": "5.0.0",
  "private": true,
  "description": "Monitor BTS com bot único e atualização via Supabase Realtime, sem F5 ou polling público",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium",
    "check": "node --check server.js && node --check monitor.js && node --check public/app.js && node --check public/admin.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  }
}